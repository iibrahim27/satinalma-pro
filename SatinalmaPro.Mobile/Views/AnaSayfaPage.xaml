<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SatinalmaPro.Mobile.ViewModels"
             xmlns:svc="clr-namespace:SatinalmaPro.Shared.Services;assembly=SatinalmaPro.Shared"
             x:Class="SatinalmaPro.Mobile.Views.AnaSayfaPage"
             x:DataType="vm:DashboardViewModel"
             Title="Panel">

    <RefreshView IsRefreshing="{Binding Yukleniyor}"
                 Command="{Binding SayfaAcildiCommand}">
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">

                <Border Style="{StaticResource MetrikCard}" Padding="20">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding PanelBasligi}" Style="{StaticResource MetrikPageTitle}" FontSize="22" />
                            <Label Text="{Binding AltBaslik}" Style="{StaticResource MetrikPageSubtitle}" />
                            <Label Text="{Binding SonGuncelleme, StringFormat='Son güncelleme: {0}'}"
                                   FontSize="11" TextColor="{AppThemeBinding Light={StaticResource MutedTextLight}, Dark={StaticResource MutedTextDark}}" />
                        </VerticalStackLayout>
                        <Border Grid.Column="1" Width="52" Height="52" StrokeShape="RoundRectangle 14"
                                StrokeThickness="0" BackgroundColor="{StaticResource PrimaryLight}">
                            <Image Source="app_logo.png" HeightRequest="36" WidthRequest="36"
                                   VerticalOptions="Center" HorizontalOptions="Center" />
                        </Border>
                    </Grid>
                </Border>

                <Label Text="Özet" Style="{StaticResource MetrikSectionTitle}" />
                <CollectionView ItemsSource="{Binding Kartlar}" SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="12" VerticalItemSpacing="12" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="svc:DashboardKart">
                            <Border Style="{StaticResource MetrikKpiCard}">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding x:DataType='vm:DashboardViewModel', Source={RelativeSource AncestorType={x:Type vm:DashboardViewModel}}, Path=KartaGitCommand}"
                                        CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>
                                <VerticalStackLayout Spacing="6">
                                    <BoxView HeightRequest="3" Color="{Binding Renk}" CornerRadius="2" />
                                    <Label Text="{Binding Deger}" FontSize="26" FontAttributes="Bold" TextColor="{Binding Renk}" />
                                    <Label Text="{Binding Baslik}" FontSize="13" FontAttributes="Bold"
                                           TextColor="{AppThemeBinding Light={StaticResource PrimaryTextLight}, Dark={StaticResource PrimaryTextDark}}" />
                                    <Label Text="{Binding AltMetin}" FontSize="11" Style="{StaticResource LblMuted}" LineBreakMode="TailTruncation" />
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Label Text="Son İşlemler" Style="{StaticResource MetrikSectionTitle}" />
                <Border Style="{StaticResource MetrikCard}" Padding="4">
                    <CollectionView ItemsSource="{Binding SonAktivite}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="svc:DashboardAktivite">
                                <Grid Padding="14,12" ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding x:DataType='vm:DashboardViewModel', Source={RelativeSource AncestorType={x:Type vm:DashboardViewModel}}, Path=AktiviteyeGitCommand}"
                                            CommandParameter="{Binding .}" />
                                    </Grid.GestureRecognizers>
                                    <VerticalStackLayout Spacing="2">
                                        <Label Text="{Binding Baslik}" FontSize="14" FontAttributes="Bold"
                                               TextColor="{AppThemeBinding Light={StaticResource PrimaryTextLight}, Dark={StaticResource PrimaryTextDark}}"
                                               LineBreakMode="TailTruncation" />
                                        <Label Text="{Binding Alt}" FontSize="12" Style="{StaticResource LblMuted}" LineBreakMode="TailTruncation" />
                                    </VerticalStackLayout>
                                    <Label Grid.Column="1" Text="{Binding Durum}" FontSize="11"
                                           TextColor="{StaticResource BrandPrimary}" VerticalOptions="Center" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>

                <Label Text="Kartlara dokunarak ilgili ekrana gidebilirsiniz."
                       FontSize="11" Style="{StaticResource LblMuted}" HorizontalTextAlignment="Center" Margin="0,0,0,8" />

            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>
