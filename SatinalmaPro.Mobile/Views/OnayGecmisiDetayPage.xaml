<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SatinalmaPro.Mobile.Views.OnayGecmisiDetayPage"
             Title="Onay Detayı">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="BtnOnayPdf" Text="Onay PDF" Clicked="OnayPdf_Clicked" Order="Primary" />
    </ContentPage.ToolbarItems>

    <RefreshView x:Name="RefreshView">
        <ScrollView>
            <VerticalStackLayout x:Name="Icerik" Spacing="8" Padding="12" />
        </ScrollView>
    </RefreshView>
</ContentPage>
