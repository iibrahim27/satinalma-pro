<UserControl x:Class="SatinalmaPro.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dash="clr-namespace:SatinalmaPro.Controls.Dashboard"
             xmlns:helpers="clr-namespace:SatinalmaPro.Helpers"
             helpers:ScrollWheelHelper.IsEnabled="True">
  <UserControl.Resources>
    <Storyboard x:Key="GirisAnimasyonu">
      <DoubleAnimation Storyboard.TargetName="Root"
                       Storyboard.TargetProperty="Opacity"
                       From="0" To="1" Duration="0:0:0.35" />
      <DoubleAnimation Storyboard.TargetName="RootTransform"
                       Storyboard.TargetProperty="(TranslateTransform.Y)"
                       From="24" To="0" Duration="0:0:0.35">
        <DoubleAnimation.EasingFunction>
          <CubicEase EasingMode="EaseOut" />
        </DoubleAnimation.EasingFunction>
      </DoubleAnimation>
    </Storyboard>
  </UserControl.Resources>

  <UserControl.Triggers>
    <EventTrigger RoutedEvent="Loaded">
      <BeginStoryboard Storyboard="{StaticResource GirisAnimasyonu}" />
    </EventTrigger>
  </UserControl.Triggers>

  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                Padding="0,0,8,24">
    <Grid x:Name="Root" Opacity="0" RenderTransformOrigin="0.5,0">
      <Grid.RenderTransform>
        <TranslateTransform x:Name="RootTransform" Y="24" />
      </Grid.RenderTransform>

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <UniformGrid x:Name="StatGrid" Grid.Row="0" Rows="1" Columns="4" Margin="0,0,0,28" />

      <StackPanel Grid.Row="1" Margin="0,0,0,28">
        <TextBlock Text="Modüller" FontSize="18" FontWeight="SemiBold"
                   Foreground="{StaticResource DashTextBrush}" Margin="0,0,0,18" />
        <WrapPanel x:Name="ModulPanel" ItemWidth="260" ItemHeight="180" />
      </StackPanel>

      <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="24" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <dash:ActivityPanelControl x:Name="ActivityPanel" Grid.Column="0" />
        <dash:StockAlertPanelControl x:Name="StockPanel" Grid.Column="2" />
      </Grid>
    </Grid>
  </ScrollViewer>
</UserControl>
